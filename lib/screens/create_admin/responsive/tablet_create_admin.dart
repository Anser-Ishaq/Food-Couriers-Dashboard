import 'package:flutter/material.dart';

class TabletCreateAdmin extends StatelessWidget {
  const TabletCreateAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}